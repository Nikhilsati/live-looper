/**
 * FXBuilder — runtime factory for FXState.
 *
 * This is the canonical home for FXBuilder.
 * It is also re-exported from `@live-looper/types` for backward compatibility.
 * New code should import from `@live-looper/audio-engine`.
 *
 * @example
 * import { FXBuilder } from "@live-looper/audio-engine";
 * const fx = new FXBuilder().build();
 */
export { FXBuilder } from "@live-looper/types";
